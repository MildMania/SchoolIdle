using System;

namespace WarHeroes.InventorySystem
{
    /*public interface IInventoryItem
    {

    }

    public abstract class InventoryItemBase<TTrackInfo, TID> : IInventoryItem
        where TTrackInfo : TrackInfo
        where TID : IConvertible
    {
        public TID ID { get; protected set; }

        public InventoryItemBase()
        {

        }

        public InventoryItemBase(
            TTrackInfo trackInfo)
        {
            InitializeFromTrackInfo(trackInfo);
        }

        protected abstract void InitializeFromTrackInfo(TTrackInfo trackInfo);

        public abstract TTrackInfo CreateTrackInfo();
    }*/
}
